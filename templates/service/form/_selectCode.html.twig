{# create a select element that transmit the option code that may be different from the displayed text #}
{# codes must be set into the input.values.code keys chain #}
{# texts must be set into the input.values.text keys chain #}

{%  extends 'service/form/tool/_select.html.twig' %}